---
const stats = [
  { number: "Laurea", label: "Universita di Bologna" },
  { number: "Focus", label: "Muscolo-scheletrico" },
  { number: "Approccio", label: "Evidence-based" },
  { number: "Servizio", label: "Domicilio e studio" },
];
import MyCv from './MyCv.astro';
---

<section class="section-shell">
  <div class="space-y-6">
    <div class="section-heading space-y-4">
      <h2 class="section-title">Un percorso serio, spiegato in modo chiaro.</h2>
      <p class="section-copy">
        Lavoro per costruire percorsi realistici, sostenibili e misurabili. La priorita e capire bene il problema, definire obiettivi concreti e scegliere il trattamento piu utile per la persona, non solo per la diagnosi.
      </p>
    </div>

    <div class="grid grid-cols-2 gap-3 md:grid-cols-4">
      {stats.map((stat) => (
        <div class="metric-card">
          <p class="text-sm uppercase tracking-[0.18em] opacity-60">{stat.number}</p>
          <p class="mt-2 font-semibold leading-snug">{stat.label}</p>
        </div>
      ))}
    </div>

    <MyCv />

    <!--
    <div class="feature-card !p-4">
      <Carousels />
    </div>
    -->
  </div>
</section>
